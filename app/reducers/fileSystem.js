import initialState from './initialState'

export default fileSystem = (state = initialState.fileSystem, action = {}) => {
  switch(action.type){
    case 'lol':
    default:
      return {...state}
  }
}